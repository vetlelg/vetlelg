You are working on a deep-sea themed portfolio site. This is one iteration
of an autonomous loop. Make progress on ONE task, then exit.

1. Read CLAUDE.md for the design system and conventions.
2. Read TODO.md and pick the top incomplete task.
3. Check `git log --oneline -5` to see what's been done.
4. Implement the task. Follow CLAUDE.md for colors, fonts, and conventions.
   Use your judgment for creative and structural decisions not covered there.
5. Run `npm run build` — fix any errors before committing.
6. Commit with `feat(section): description` or `fix(section): description`.
7. Update TODO.md: mark the task [x], move it to Completed with today's date.
   Add anything you noticed under Discovered issues.
8. Exit.

Rules:
- One task per iteration. If it's too big, break it into sub-tasks in TODO.md
  and complete the first one.
- Don't modify CLAUDE.md.
- Don't refactor code outside the current task's scope.

When every task is done, write ALL_TASKS_COMPLETE as the first line of TODO.md.
